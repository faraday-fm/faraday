export type CommandOptions = {
  name?: string;
  when?: string;
  whenFocus?: boolean;
  whenFocusWithin?: boolean;
};

export type ContextOptions = {
  name?: string;
  whenFocus?: boolean;
  whenFocusWithin?: boolean;
};
